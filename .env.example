# Oura OAuth Configuration
# Get these from https://cloud.ouraring.com/oauth/applications
OURA_CLIENT_ID=your_oura_client_id_here
OURA_CLIENT_SECRET=your_oura_client_secret_here

# Steno Chat Configuration
# This is the ID of your Steno chat instance
STENO_CHAT_ID=your_chat_id_here
NEXT_PUBLIC_STENO_CHAT_ID=your_chat_id_here

# NextJS Configuration
# Generate a random string for NEXTAUTH_SECRET: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# For local development OAuth (use ngrok or similar)
# Uncomment and set this when using ngrok for local OAuth testing
# NGROK_URL=https://your-ngrok-subdomain.ngrok.app

# Deployment URLs (will be automatically set by Vercel)
# VERCEL_URL is automatically set by Vercel during deployment
# PRODUCTION_URL=https://your-production-domain.com

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env.local
#    cp .env.example .env.local
#
# 2. Fill in your Oura OAuth credentials
#    - Go to https://cloud.ouraring.com/oauth/applications
#    - Create a new application
#    - Add redirect URIs:
#      - For local: https://your-ngrok-url.ngrok.app/auth/oura
#      - For production: https://your-domain.com/auth/oura
#
# 3. Set your Steno Chat ID
#    - Use the ID from your Steno chat configuration
#
# 4. For local development with OAuth:
#    - Install ngrok: npm install -g ngrok
#    - Run: ngrok http 3000
#    - Set NGROK_URL to the HTTPS URL provided
#
# 5. For production:
#    - Deploy to Vercel
#    - Add these environment variables in Vercel dashboard
#    - Update Oura OAuth redirect URIs to match your domain